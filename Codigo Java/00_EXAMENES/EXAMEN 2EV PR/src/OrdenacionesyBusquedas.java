import java.util.ArrayList;
import java.util.Collections;
/*
public class OrdenacionesyBusquedas {

    public static Persona busquedaBinaria(ArrayList<Persona> lista, String nombreBuscado) {
        int inicio = 0;
        int fin = lista.size() - 1;
        while (inicio <= fin) {
            int medio = inicio + (fin - inicio) / 2;

            if (lista.get(medio).getNombre().equalsIgnoreCase(nombreBuscado)) {
                return lista.get(medio);
            } else if (lista.get(medio).getNombre().compareTo(nombreBuscado) < 0) {
                inicio = medio + 1;
            } else {
                fin = medio - 1;
            }
        }
        return null; // Si no se encuentra el objeto, devolver null

}

    public static void ordenarPorAtributo(ArrayList<Paciente> lista, String atributo) {
        for (int i = 0; i < lista.size() - 1; i++) {
            for (int j = i + 1; j < lista.size(); j++) {
                Paciente p1 = lista.get(i);
                Paciente p2 = lista.get(j);

                if (atributo.equalsIgnoreCase("nombre")) {
                    if (p1.getNombre().compareTo(p2.getNombre()) > 0) {
                        Collections.swap(lista, i, j);
                    }
                }
                if (atributo.equalsIgnoreCase("apellido")) {
                    if (p1.getNombre().compareTo(p2.getNombre()) > 0) {
                        Collections.swap(lista, i, j);
                    }
                }
                if (atributo.equalsIgnoreCase("dni")) {
                    if (p1.getNombre().compareTo(p2.getNombre()) > 0) {
                        Collections.swap(lista, i, j);
                    }
                }
            }
        }
    }


}


*/
